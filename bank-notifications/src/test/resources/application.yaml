spring:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false

resilience4j:
  circuitbreaker:
    enabled: false
